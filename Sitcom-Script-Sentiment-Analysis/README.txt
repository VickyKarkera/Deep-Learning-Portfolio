The "CS688_Final_Term_Project_Code_(Preprocessing_+_Sentiment_Analysis).ipynb" script scrapes the transcript website and conducts sentiment analysis on each episode and assigns it sentiment scores, the end result is the final script dataset with each episode having different scores for all emotions.

One does not need to run the above script, as the script has been extracted already and is within the friends web app/data directory.

its import to install streamlit package using "pip install streamlit openpyxl"

To Run the app you should open friends web app folder on commandline and type the following command to run the app on localhost "streamlit run friends_episode_emotion_app\Home.py" 

Now the app is opened successfully!